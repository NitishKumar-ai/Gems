# Prisma resolves this relative to prisma/schema.prisma, so "file:./dev.db" means
# prisma/dev.db. Create it with: npx prisma db push
DATABASE_URL="file:./dev.db"

# NextAuth (GitHub provider). Generate a secret with: openssl rand -base64 32
AUTH_SECRET=""
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""
